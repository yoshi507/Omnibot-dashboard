# DISABLED — this workflow previously uploaded the unbuilt source tree to Pages.
# The Vite app must be built first; use deploy-pages.yml instead.
name: Disabled static upload (do not enable)

on:
  workflow_dispatch:

jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use Deploy to GitHub Pages."
